global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
{% for host in groups['webservers'] %}
  - job_name: '{{ host }}'
    static_configs:
      - targets: ['{{ host }}:{{ node_exporter_port | default(9100) }}']
        labels:
          instance: '{{ host }}'
{% endfor %}
